# Canonical NFL Ontology Foundation

This is the first shared vocabulary layer for the Football Intelligence OS. It intentionally separates canonical concepts from team aliases. A team may map its terminology to a canonical term, but an alias cannot silently redefine the canonical concept.

The ontology is versioned and is a dependency of player instruction, playbook records, film tags, scouting, analytics, and agent routing.
