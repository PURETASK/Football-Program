{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nfl-fidos.local/contracts/organization-special-teams.schema.json",
  "title": "NFL FIDOS Organization Special Teams Personnel Package",
  "type": "object",
  "required": ["id", "organization_id", "team_context", "season", "assignments", "status", "human_review_required"],
  "properties": {
    "id": {"type": "string", "pattern": "^ORG-SPECIAL-TEAMS-"},
    "organization_id": {"type": "string", "pattern": "^ORG-"},
    "team_context": {"type": "string", "minLength": 1},
    "season": {"type": "string", "minLength": 1},
    "assignments": {"type": "array", "minItems": 1, "items": {"type": "object"}},
    "status": {"enum": ["under_review", "validated", "rejected"]},
    "human_review_required": {"type": "boolean"},
    "approved_by": {"type": ["string", "null"]},
    "owner_decision_ref": {"type": ["string", "null"]}
  },
  "additionalProperties": true
}
